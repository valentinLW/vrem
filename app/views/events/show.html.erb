<%= render "show_container", event:@event %>

<% if @event.user_id == current_user.id %>
  <%= link_to(event_invitations_path(@event), method: :get, :class => 'btn-boton') do %>
    <p>Add People</p>
  <% end %>
<% end %>


<% if @invitation && @invitation.status == "pending" %>
  <%= link_to(accept_path(@invitation), method: :patch, :class => 'btn-boton') do %>
    <p>Accept</p>
  <% end %>
  <%= link_to(reject_path(@invitation), method: :patch, :class => 'btn-boton') do %>
    <p>Reject</p>
  <% end %>
<% end %>

<% if @event.end_time > Date.today %>
    <div>
      <div class="links">
        <%= link_to((event_chat_path(@event)), :class => 'btn-boton') do %>
          <p>CHAT</p>
        <% end%>
    </div>
<% else %>
    <div>
      <div class="links">
        <%= link_to((event_chat_path(@event)), :class => 'btn-boton') do %>
          MEDIA
        <% end%>
      </div>
<% end %>

<% if @event.start_time < Date.today %>
    <div>
      <div class="links">
        <%= link_to((event_chat_path(@event)), :class => 'btn-boton') do %>
          Upload Photos
        <% end%>
    </div>
<% end %>

<div class="links">
  <%= link_to(events_path, :class => 'btn-boton') do %>
    Dashboard
  <% end%>
</div>
