<div class="message-container" id="message-<%= message.id %>">
  <p class="<%= message.user == current_user ? 'others' : 'me' %>"><%= message.content %></p>
  <i class="nickname <%= message.user == current_user ? 'others' : 'me' %>"><%= message.user.nickname %></i>
</div>
